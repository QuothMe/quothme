<ul>
<li> <b> <%= image_tag comment.user.profile_picture.comment_thumb%> <%= comment.user.first_name%> <%= comment.user.last_name%>:</b>
<span> <%= comment.content%>

<% if comment.user == current_user %>
	 <%= link_to 'delete', quote_comment_path(quote, comment), method: :delete, data: {confirm: "Are you sure?"} %>

<% end %>

</span></li>
</ul>
